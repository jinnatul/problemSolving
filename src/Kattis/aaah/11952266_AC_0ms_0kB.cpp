#include<bits/stdc++.h>
#include<stdio.h>
#include<string.h>
#include<math.h>
#include<algorithm>
#include<vector>

using namespace std;

int main()
{
	 int n,i,a,b;
	 char arr[1000],brr[1000];
	 cin>>arr>>brr;
     
     int ln = strlen(arr);
     int ln2 = strlen(brr);
     if(ln>=ln2){
     	cout<<"go"<<endl;
     }
     else{
     	cout<<"no"<<endl;
     }

}